"""Shared, cross-app helper functions for the StepsDoor backend.

Placeholder module: no helpers have been added yet. Intended home for
small, stateless utility functions (e.g. string/date formatting, slug
generation) that would otherwise be duplicated across multiple apps
(jobs, tenders, govtjobs, etc.) — as opposed to `core.affiliate`
(affiliate-link-specific logic) or `core.validators` (field/model
validators), which are kept in their own modules.
"""

# Utility functions for the StepsDoor backend.
# Add shared helper functions here as the project grows.
