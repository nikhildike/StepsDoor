# Marks `core/` as a Python package. `core` holds shared, cross-app utilities
# (permissions, pagination, exception handling, validators, affiliate link
# building) that are imported by apps throughout the StepsDoor backend — it
# intentionally has no app-specific logic of its own.
