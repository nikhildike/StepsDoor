# Package marker for the `notifications` Django app (FCM device-token registration
# for push notifications; email dispatch is handled via SendGrid/django-anymail
# elsewhere in the project). Intentionally empty beyond this comment.
