# Package marker for the `alerts` Django app (private job alerts: saved search
# criteria that a Celery task matches against newly posted jobs to trigger
# email/push notifications). Intentionally empty beyond this comment.
